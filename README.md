# Seller Product MFE

Product list, creation, editing, categories, and variation management. Extracted from `seller-mfe` at commit `572b00b8dc02a9ec242a9506f90394ede2b988e4`.

Exposes only `seller_product/Routes`. The standalone development entry supplies its own router and providers; the federated entry consumes them from the shell.
